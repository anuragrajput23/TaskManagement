{
  "name": "task-management",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "build": "npm --workspace frontend run build",
    "start": "npm --workspace backend start"
  },
  "engines": {
    "node": "22.x"
  }
}
